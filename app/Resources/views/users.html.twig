<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		
	
<h1>user List</h1>

			<ul>
				{% for user in users %}
					<li>Email: {{ user.email }} Name: {{ user.name }} Phone: {{ user.phone }}</li>
				{% else %}
					<li><em>no user found</em></li>
				{% endfor %}
			</ul>

			


    </body>
</html>
